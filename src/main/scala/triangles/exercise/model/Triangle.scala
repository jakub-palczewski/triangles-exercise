package triangles.exercise.model

/** Represents the triangle of numbers
  *
  * @param rows
  *   The triangle data, where rows(i) contains i+1 elements
  */
case class Triangle(rows: Vector[Vector[Int]])
